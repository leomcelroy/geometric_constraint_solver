import { Stream } from './stream';
import { Token, TokenType } from './tokenizer';
export declare class ParserContext {
    private _tokens;
    constructor(_tokens: Stream<Token>);
    hasMoreTokens(): boolean;
    advanceToNextTokenOfType(tokenType: TokenType): boolean;
    consumeIfType(tokenType: TokenType): Token | null;
    consumeIf(predicate: (token: Token) => boolean): Token | null;
    peek(): Token;
    next(): Token;
}
