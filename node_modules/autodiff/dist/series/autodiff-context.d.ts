import { Pool } from './pool';
import { Series } from './series';
import { Series2D } from './series2d';
export interface Options {
    numDerivativesToCompute: number;
}
export declare class AutodiffContext {
    private _numDerivativesToCompute;
    seriesPool: Pool<Series>;
    series2DPool: Pool<Series2D>;
    constructor(opts: Options);
    setNumberOfDerivativesToCompute(n: number): void;
    numDerivativesToCompute(): number;
}
export declare const defaultContext: AutodiffContext;
